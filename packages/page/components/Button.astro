---
// Button.astro
export interface Props {
  variant?: "default" | "primary" | "success" | "warning" | "error" | "ghost";
  size?: "sm" | "md" | "lg";
  icon?: string;
  href?: string;
  target?: string;
  disabled?: boolean;
}

const {
  variant = "default",
  size = "md",
  icon,
  href,
  target,
  disabled = false,
} = Astro.props;

const Tag = href ? "a" : "button";
const attrs = href ? { href, target } : {};
const buttonAttrs = !href && disabled ? { disabled } : {};
---

<Tag
  class={`page-button page-button-${variant} page-button-${size}`}
  {...attrs}
  {...buttonAttrs}
>
  {icon && <span class="page-button-icon">{icon}</span>}
  <span class="page-button-text">
    <slot />
  </span>
</Tag>

<style>
  .page-button {
    display: inline-flex;
    margin-top: 0;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 12px 20px;
    border-radius: 8px;
    font-weight: 500;
    font-size: 14px;
    line-height: 1;
    text-decoration: none;
    border: 1px solid var(--page-border);
    cursor: pointer;
    transition: all 0.2s ease;
    white-space: nowrap;
    user-select: none;
    background: var(--page-bg-secondary);
    color: var(--page-text);
    border-color: var(--page-border);
  }

  /* Remove p tag style inside slot */
  .page-button p {
    margin: 0;
    padding: 0;
    color: inherit;
    font-weight: inherit;
    font-size: inherit;
    line-height: inherit;
    text-decoration: inherit;
    background: inherit;
    border: inherit;
  }

  /* Force via global style */
  :global(.page-button p) {
    margin: 0;
    padding: 0;
    color: inherit;
    font-weight: inherit;
    font-size: inherit;
    line-height: inherit;
    text-decoration: inherit;
    background: inherit;
    border: inherit;
  }

  /* Override markdown style with stronger selector */
  :global(.page-content .sl-markdown-content .page-button p) {
    margin: 0;
    padding: 0;
    color: inherit;
    font-weight: inherit;
    font-size: inherit;
    line-height: inherit;
    text-decoration: inherit;
    background: inherit;
    border: inherit;
  }

  /* Force inherit for all inner elements */
  .page-button * {
    color: inherit;
    font-weight: inherit;
    font-size: inherit;
    line-height: inherit;
    text-decoration: inherit;
    background: inherit;
    border: inherit;
  }

  /* Global style force inherit for button inner elements */
  :global(.page-button *) {
    color: inherit;
    font-weight: inherit;
    font-size: inherit;
    line-height: inherit;
    text-decoration: inherit;
    background: inherit;
    border: inherit;
  }

  .page-button:hover:not(:disabled) {
    background: var(--page-bg-elevated);
    border-color: var(--page-border-strong);
    transform: translateY(-1px);
    box-shadow: var(--page-shadow-sm);
  }

  .page-button:active:not(:disabled) {
    transform: translateY(0);
    transition: transform 0.1s ease;
  }

  .page-button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
    transform: none;
  }

  /* Link button style */
  .page-button[href] {
    text-decoration: none;
    color: inherit;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 12px 20px;
    border-radius: 8px;
    font-weight: 500;
    font-size: 14px;
    line-height: 1;
    border: 1px solid var(--page-border);
    cursor: pointer;
    transition: all 0.2s ease;
    white-space: nowrap;
    user-select: none;
    background: var(--page-bg-secondary);
    color: var(--page-text);
    border-color: var(--page-border);
    margin-top: 1rem;
  }

  .page-button[href]:hover {
    text-decoration: none;
    color: inherit;
    background: var(--page-bg-elevated);
    border-color: var(--page-border-strong);
    transform: translateY(-1px);
    box-shadow: var(--page-shadow-sm);
  }

  .page-button[href]:focus {
    outline: 2px solid var(--page-accent);
    outline-offset: 2px;
  }

  .page-button[href]:active {
    transform: translateY(0);
    box-shadow: var(--page-shadow-xs);
  }

  .page-button[href]:hover::after {
    width: 0;
  }

  [data-theme="dark"] .page-button[href] {
    background: var(--page-bg-secondary);
    color: var(--page-text);
    border-color: var(--page-border);
  }

  [data-theme="dark"] .page-button[href]:hover {
    background: var(--page-bg-elevated);
    border-color: var(--page-border-strong);
    color: var(--page-text);
  }

  [data-theme="dark"] .page-button[href]:focus {
    outline-color: var(--page-accent);
  }

  /* Link button variant styles */
  .page-button[href].page-button-primary {
    background: var(--blue-600);
    border-color: var(--blue-600);
    color: var(--neutral-white);
  }

  .page-button[href].page-button-primary:hover {
    background: var(--blue-700);
    border-color: var(--blue-700);
    color: var(--neutral-white);
  }

  .page-button[href].page-button-success {
    background: var(--green-600);
    border-color: var(--green-600);
    color: var(--neutral-white);
  }

  .page-button[href].page-button-success:hover {
    background: var(--green-700);
    border-color: var(--green-700);
    color: var(--neutral-white);
  }

  .page-button[href].page-button-warning {
    background: var(--orange-600);
    border-color: var(--orange-600);
    color: var(--neutral-white);
  }

  .page-button[href].page-button-warning:hover {
    background: var(--orange-700);
    border-color: var(--orange-700);
    color: var(--neutral-white);
  }

  .page-button[href].page-button-error {
    background: var(--red-600);
    border-color: var(--red-600);
    color: var(--neutral-white);
  }

  .page-button[href].page-button-error:hover {
    background: var(--red-700);
    border-color: var(--red-700);
    color: var(--neutral-white);
  }

  .page-button[href].page-button-ghost {
    background: transparent;
    border-color: var(--page-border);
    color: var(--page-text);
  }

  .page-button[href].page-button-ghost:hover {
    background: var(--page-bg-secondary);
    border-color: var(--page-border-strong);
    color: var(--page-text);
  }

  /* Dark mode link button variant */
  [data-theme="dark"] .page-button[href].page-button-primary {
    background: var(--blue-500);
    border-color: var(--blue-500);
    color: var(--neutral-white);
  }

  [data-theme="dark"] .page-button[href].page-button-primary:hover {
    background: var(--blue-600);
    border-color: var(--blue-600);
    color: var(--neutral-white);
  }

  [data-theme="dark"] .page-button[href].page-button-success {
    background: var(--green-500);
    border-color: var(--green-500);
    color: var(--neutral-white);
  }

  [data-theme="dark"] .page-button[href].page-button-success:hover {
    background: var(--green-600);
    border-color: var(--green-600);
    color: var(--neutral-white);
  }

  [data-theme="dark"] .page-button[href].page-button-warning {
    background: var(--orange-500);
    border-color: var(--orange-500);
    color: var(--neutral-white);
  }

  [data-theme="dark"] .page-button[href].page-button-warning:hover {
    background: var(--orange-600);
    border-color: var(--orange-600);
    color: var(--neutral-white);
  }

  [data-theme="dark"] .page-button[href].page-button-error {
    background: var(--red-500);
    border-color: var(--red-500);
    color: var(--neutral-white);
  }

  [data-theme="dark"] .page-button[href].page-button-error:hover {
    background: var(--red-600);
    border-color: var(--red-600);
    color: var(--neutral-white);
  }

  [data-theme="dark"] .page-button[href].page-button-ghost {
    background: transparent;
    border-color: var(--page-border);
    color: var(--page-text);
  }

  [data-theme="dark"] .page-button[href].page-button-ghost:hover {
    background: var(--page-bg-secondary);
    border-color: var(--page-border-strong);
    color: var(--page-text);
  }

  .page-button-icon {
    font-size: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.2s ease;
    border: none;
    outline: none;
    background: transparent;
  }

  .page-button:hover .page-button-icon {
    transform: scale(1.1);
  }

  .page-button-text {
    color: inherit;
    font-weight: inherit;
  }

  /* Remove icon border via global style */
  :global(.page-button .page-button-icon) {
    border: none;
    outline: none;
    background: transparent;
    box-shadow: none;
  }

  :global(.page-button-text) {
    border: none;
    outline: none;
    background: transparent;
    box-shadow: none;
  }

  /* Default Button */
  .page-button-default {
    background: var(--page-bg-secondary);
    border-color: var(--page-border);
    color: var(--page-text);
  }

  .page-button-default:hover:not(:disabled) {
    background: var(--page-bg-elevated);
    border-color: var(--page-border-strong);
    color: var(--page-text);
  }

  :global([data-theme="dark"]) .page-button-default {
    background: var(--page-bg-secondary);
    border-color: var(--page-border);
    color: var(--page-text);
  }

  :global([data-theme="dark"]) .page-button-default:hover:not(:disabled) {
    background: var(--page-bg-elevated);
    border-color: var(--page-border-strong);
    color: var(--page-text);
  }

  /* Primary Button */
  .page-button-primary {
    background: var(--blue-600);
    border-color: var(--blue-600);
    color: var(--neutral-white);
  }

  .page-button-primary:hover:not(:disabled) {
    background: var(--blue-700);
    border-color: var(--blue-700);
    color: var(--neutral-white);
  }

  :global([data-theme="dark"]) .page-button-primary {
    background: var(--blue-500);
    border-color: var(--blue-500);
    color: var(--neutral-white);
  }

  :global([data-theme="dark"]) .page-button-primary:hover:not(:disabled) {
    background: var(--blue-600);
    border-color: var(--blue-600);
    color: var(--neutral-white);
  }

  /* Success Button */
  .page-button-success {
    background: var(--green-600);
    border-color: var(--green-600);
    color: var(--neutral-white);
  }

  .page-button-success:hover:not(:disabled) {
    background: var(--green-700);
    border-color: var(--green-700);
    color: var(--neutral-white);
  }

  :global([data-theme="dark"]) .page-button-success {
    background: var(--green-500);
    border-color: var(--green-500);
    color: var(--neutral-white);
  }

  :global([data-theme="dark"]) .page-button-success:hover:not(:disabled) {
    background: var(--green-600);
    border-color: var(--green-600);
    color: var(--neutral-white);
  }

  /* Warning Button */
  .page-button-warning {
    background: var(--orange-600);
    border-color: var(--orange-600);
    color: var(--neutral-white);
  }

  .page-button-warning:hover:not(:disabled) {
    background: var(--orange-700);
    border-color: var(--orange-700);
    color: var(--neutral-white);
  }

  :global([data-theme="dark"]) .page-button-warning {
    background: var(--orange-500);
    border-color: var(--orange-500);
    color: var(--neutral-white);
  }

  :global([data-theme="dark"]) .page-button-warning:hover:not(:disabled) {
    background: var(--orange-600);
    border-color: var(--orange-600);
    color: var(--neutral-white);
  }

  /* Error Button */
  .page-button-error {
    background: var(--red-600);
    border-color: var(--red-600);
    color: var(--neutral-white);
  }

  .page-button-error:hover:not(:disabled) {
    background: var(--red-700);
    border-color: var(--red-700);
    color: var(--neutral-white);
  }

  :global([data-theme="dark"]) .page-button-error {
    background: var(--red-500);
    border-color: var(--red-500);
    color: var(--neutral-white);
  }

  :global([data-theme="dark"]) .page-button-error:hover:not(:disabled) {
    background: var(--red-600);
    border-color: var(--red-600);
    color: var(--neutral-white);
  }

  /* Ghost Button */
  .page-button-ghost {
    background: transparent;
    border-color: var(--page-border);
    color: var(--page-text);
  }

  .page-button-ghost:hover:not(:disabled) {
    background: var(--page-bg-secondary);
    border-color: var(--page-border-strong);
    color: var(--page-text);
  }

  :global([data-theme="dark"]) .page-button-ghost {
    background: transparent;
    border-color: var(--page-border);
    color: var(--page-text);
  }

  :global([data-theme="dark"]) .page-button-ghost:hover:not(:disabled) {
    background: var(--page-bg-secondary);
    border-color: var(--page-border-strong);
    color: var(--page-text);
  }

  /* Sizes */
  .page-button-sm {
    padding: 8px 16px;
    font-size: 12px;
    border-radius: 8px;
  }

  .page-button-sm .page-button-icon {
    font-size: 14px;
  }

  .page-button-md {
    padding: 12px 20px;
    font-size: 14px;
    border-radius: 12px;
  }

  .page-button-md .page-button-icon {
    font-size: 16px;
  }

  .page-button-lg {
    padding: 16px 28px;
    font-size: 16px;
    border-radius: 14px;
  }

  .page-button-lg .page-button-icon {
    font-size: 18px;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .page-button {
      padding: 10px 18px;
      font-size: 13px;
    }

    .page-button-lg {
      padding: 14px 24px;
      font-size: 15px;
    }
  }

  /* Animation */
  @keyframes buttonPulse {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
    100% {
      transform: scale(1);
    }
  }

  .page-button:active:not(:disabled) {
    animation: buttonPulse 0.2s ease;
  }
</style>
