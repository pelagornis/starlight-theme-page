---
// Tabs.astro
export interface Props {
  tabs: Array<{
    id: string;
    label: string;
    icon?: string;
  }>;
  defaultTab?: string;
  id?: string;
}

const {
  tabs,
  defaultTab = tabs[0]?.id,
  id = `tabs-${Math.random().toString(36).substr(2, 9)}`,
} = Astro.props;
---

<div class="page-tabs" data-tabs-id={id}>
  <div class="page-tabs-header">
    {
      tabs.map((tab) => (
        <button
          class={`page-tab-button ${tab.id === defaultTab ? "active" : ""}`}
          data-tab={tab.id}
          data-tabs-id={id}
        >
          {tab.icon && <span class="tab-icon">{tab.icon}</span>}
          <span class="tab-label">{tab.label}</span>
        </button>
      ))
    }
  </div>

  <div class="page-tabs-content">
    <div class="page-tab-panels">
      <slot />
    </div>
  </div>
</div>

<style>
  /* ===== 토스 스타일 탭 ===== */
  .page-tabs {
    margin: 2rem 0;
  }

  .page-tabs-header {
    display: flex;
    gap: 8px;
    margin-bottom: 20px;
    padding: 4px;
    background: var(--page-bg-secondary);
    border-radius: 12px;
    border: 1px solid var(--page-border);
  }

  /* 탭 버튼 */
  .page-tab-button {
    flex: 1;
    margin-top: 0 !important;
    padding: 12px 16px;
    background: transparent;
    border: none;
    color: var(--page-text-secondary);
    font-weight: 500;
    font-size: 14px;
    cursor: pointer;
    transition: all 0.2s ease;
    border-radius: 8px;
    white-space: nowrap;
    display: flex;
    align-items: center;
    gap: 8px;
    justify-content: center;
    position: relative;
  }

  .page-tab-button:hover {
    color: var(--page-text);
    background: var(--page-bg);
  }

  .page-tab-button.active {
    color: var(--page-text);
    background: var(--page-bg);
    font-weight: 600;
    box-shadow: var(--page-shadow-sm);
  }

  /* 다크모드 스타일 */
  [data-theme="dark"] .page-tabs-header {
    background: var(--page-bg-secondary);
    border-color: var(--page-border);
  }

  [data-theme="dark"] .page-tab-button {
    color: var(--page-text-secondary);
  }

  [data-theme="dark"] .page-tab-button:hover {
    color: var(--page-text);
    background: var(--page-bg-elevated);
  }

  [data-theme="dark"] .page-tab-button.active {
    color: var(--page-text);
    background: var(--page-bg-elevated);
    box-shadow: var(--page-shadow-md);
  }

  /* 탭 아이콘 */
  .tab-icon {
    font-size: 16px;
    width: 16px;
    height: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .tab-label {
    font-size: 14px;
    font-weight: inherit;
  }

  /* 탭 콘텐츠 */
  .page-tabs-content {
    position: relative;
  }

  .page-tab-panels {
    position: relative;
  }

  .page-tab-panels > * {
    display: none;
    opacity: 0;
    transition: opacity 0.2s ease;
  }

  /* 반응형 디자인 */
  @media (max-width: 768px) {
    .page-tab-button {
      padding: 10px 12px;
      font-size: 13px;
    }

    .tab-icon {
      font-size: 14px;
      width: 14px;
      height: 14px;
    }
  }

  /* 포커스 스타일 */
  .page-tab-button:focus-visible {
    outline: 2px solid var(--page-accent);
    outline-offset: 2px;
  }
</style>

<script>
  // 탭 전환 기능
  document.addEventListener("DOMContentLoaded", function () {
    const tabContainers = document.querySelectorAll(".page-tabs");

    tabContainers.forEach((container) => {
      const tabsId = container.getAttribute("data-tabs-id");
      const tabButtons = container.querySelectorAll(
        `[data-tabs-id="${tabsId}"]`
      );
      const tabPanels = container.querySelectorAll(".page-tab-panels > *");

      // 초기 상태 설정: 모든 탭 패널 숨기기
      tabPanels.forEach((panel) => {
        (panel as HTMLElement).style.display = "none";
        (panel as HTMLElement).style.opacity = "0";
      });

      // 첫 번째 탭만 활성화
      if (tabButtons.length > 0 && tabPanels.length > 0) {
        tabButtons[0].classList.add("active");
        (tabPanels[0] as HTMLElement).style.display = "block";
        (tabPanels[0] as HTMLElement).style.opacity = "1";
      }

      tabButtons.forEach((button, index) => {
        button.addEventListener("click", () => {
          // 현재 탭 컨테이너의 모든 탭 버튼 비활성화
          tabButtons.forEach((btn) => btn.classList.remove("active"));
          // 현재 탭 컨테이너의 모든 탭 패널 숨기기
          tabPanels.forEach((panel) => {
            (panel as HTMLElement).style.display = "none";
            (panel as HTMLElement).style.opacity = "0";
          });

          // 선택된 탭 활성화
          button.classList.add("active");
          if (tabPanels[index]) {
            (tabPanels[index] as HTMLElement).style.display = "block";
            (tabPanels[index] as HTMLElement).style.opacity = "1";
          }
        });
      });
    });
  });
</script>
